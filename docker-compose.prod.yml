services:
  backend:
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000
    volumes: []
    restart: always

  frontend:
    build:
      dockerfile: Dockerfile.prod
    volumes: []
    ports:
      - "80:80"
    environment: {}

volumes:
  pgdata:
